import React from "react";
import ReactDOM from 'react-dom';
import App from "./App/App";

// Root App
const root = ReactDOM.createRoot(document.getElementById('root'));   // notice the '!'
root.render(<App />);
